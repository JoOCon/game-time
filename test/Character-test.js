const { expect } = require('chai');
const Frogger = require('../lib/Frogger.js');


describe('Character', () => {
  // Setup
  const frog = new Frogger(200, 200, 20, 20);
  // Execution

  // Expect
});