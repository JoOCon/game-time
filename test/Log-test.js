const { expect } = require('chai');
const Log = require('../lib/Log.js');

describe('Log test suite', () => {
  it('should instantiate Logs at the correct coordinates', () => {
    // Setup

    // Execution

    // Expectation

  });
});