const { expect } = require('chai');
const Log = require('../lib/Log.js');

describe('Log test suite', () => {
  it('should instantiante Logs at the correct coordinates', () => {
    // Setup

    // Execution

    // Expectation

  });
});